{
  "name": "2023_1_wap_web_team6",
  "version": "1.0.0",
  "description": "<div align = \"center\">",
  "main": "index.js",
  "scripts": {
    "client-build": "cd cookhelp && cd front && yarn install && yarn build && yarn start && cd ../",
    "server-build": "cd server && yarn install && yarn build && cd ../",
    "heroku-postbuild" : "yarn client-build && yarn server-build && mv ./front/build .server/build",
    "start" : "cd server && yarn server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "heroku": "^8.1.7",
    "pkg": "^5.8.1"
  }
}