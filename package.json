{
  "name": "2023_1_wap_web_team6",
  "version": "1.0.0",
  "description": "<div align = \"center\">",
  "main": "index.js",
  "scripts": {
    "start" : "cd cookhelp && yarn install && cd server && yarn install && cd ../front && yarn install && cd .. && yarn dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "heroku": "^8.1.7",
    "pkg": "^5.8.1"
  }
}
